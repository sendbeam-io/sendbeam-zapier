## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## Why

<!-- What was wrong, or what was misleading. Not what you typed — the diff already says that. -->

## Checks

- [ ] `npm test` passes.
- [ ] `npm run validate` passes.
- [ ] A behaviour change is pinned by a test, and a new trigger, action or search has a scenario in `test/contract.test.js`.
- [ ] No new runtime dependency.
- [ ] If this changes a trigger, action, search or the permissions a key needs, the README tables say so.
- [ ] If people using the app will notice the change, `CHANGELOG.md` says so.
